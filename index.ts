function halfFryEgg(): number {
  //after parenthensis is the type of return if type is define the must use the return
  let cooked: number = 1 + 1.5 + 2;
  // salt + butter + egg
  console.log(cooked);
  return cooked;
}
halfFryEgg();

const respnse = halfFryEgg();
console.log(respnse); // invoking function
function myTeachers(
  teacher1: string,
  teacher2: string,
  teacher3: string,
  teacher4: string
) {
  return teacher1 + teacher2 + teacher3 + teacher4;
}

const response = myTeachers("Asharib", "Ubaid", "Hafsa", "Naeem");
console.log(response);
